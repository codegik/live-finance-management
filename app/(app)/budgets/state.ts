export type BudgetState = { error: string | null; message: string | null }

export const SAVED_MESSAGE = 'Budgets saved.'
export const INVALID_AMOUNT_ERROR = 'One of those amounts is not a number.'
export const UNKNOWN_CATEGORY_ERROR = 'That category no longer exists.'
export const INVALID_PERIOD_ERROR = 'That is not a month this form can save.'
